<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.UserDao">

  <!--<resultMap id="PartUserInfo" type="java.util.Map">
    <id column="id" property="id"></id>
    <result column="username" property="username"></result>
    <result column="created_at" property="createdAt"></result>
  </resultMap>

  &lt;!&ndash;  配置查询所有&ndash;&gt;
  <select id="findAll" resultType="mybatis.User">
        select * from gbox_manager.users
    </select>

  <update id="updateUserPhone">
     update gbox_manager.users set phone = #{phone} where id=#{id}
  </update>

&lt;!&ndash;  根据条件查询&ndash;&gt;
  <select id="findUserByCondition" parameterType="mybatis.User" resultType="mybatis.User">
    select * from gbox_manager.users
    <where>
      <if test="username != null">
        and username = #{username}
      </if>
      <if test="createdAt != null">
        and created_at = #{createdAt}
      </if>
    </where>
  </select>

&lt;!&ndash; select in&ndash;&gt;
  <select id="findUserInIds" resultMap="PartUserInfo">
    select id, username, created_at from gbox_manager.users where id in
    <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
      #{item}
    </foreach>
  </select>
-->
  <!--  <insert-->
  <!--    id="saveUser" parameterType="com.test.springtest.User"-->
  <!--    useGeneratedKeys="true">-->
  <!--        insert into student(NAME,AGE) values (#{name},#{age})-->
  <!--    </insert>-->
</mapper>
