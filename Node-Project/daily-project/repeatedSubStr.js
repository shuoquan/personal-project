const repeatedSubstringPattern = (s) => {
  const flag = false;
  let start = s.length;
  // while (!start % 2) {
  //     const temp = s.slice(0, start / 2);
  //     let index = start / 2;
  //     let
  //     while(index < s.length) {
  //         if()
  //     }
  // }
  return flag;
  // if (s.length % 2) {
  //   return false;
  // } else {
  //   const flag = false;
  //   const start = s.length / 2;
  //   while (start) {}
  // }
};


console.log(Number.MAX_VALUE)
